apiVersion: v1
kind: Service
metadata:
  name: leave-management-service
spec:
  selector:
    app: leave-management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer